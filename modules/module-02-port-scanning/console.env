# Module 02 console descriptor — sourced by scripts/lab-console.
#
# SHELL MODE: this is a multi-host module, but the student does all the work from
# ONE real machine — the attacker workstation. Setting SHELL_HOST logs them
# straight into a real interactive shell there (root, with nmap + raw sockets),
# where they run genuine nmap against the real lab network 10.2.0.0/24 — no lab>
# verbs. The welcome banner, themed prompt and `labhelp` live in the attacker
# image (modules/module-02-port-scanning/attacker/).
MODULE_NO=02
MODULE_TITLE="Port Scanning & Enumeration"
MODULE_SUBTITLE="Penetration Testing Workstation"
UP_CHECK="m02-attacker"
HOSTS="web files app attacker"
SHELL_HOST="attacker"
