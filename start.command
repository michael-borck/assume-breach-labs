#!/bin/bash
# macOS: double-click this file to launch the Assume Breach labs in Terminal.
# First time only: if macOS blocks it, right-click the file → Open → Open.
cd "$(dirname "$0")" || exit 1
exec ./start.sh
